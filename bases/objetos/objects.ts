(() => {
  
    let flash = {
        name: 'Barry Allen',
        age: 24,
        powers: ['Viajar en el tiempo', 'Super velocidad']
    }

    //No podremos llegar y agregar propiedades que no las hay, a pesar que es valido hacerlo, mutar un objeto.
    //Es necesario crear objetos con tipos, para la proxima lección.

    /**
     * * Por ejemplo
     * flash={
     * name: 'Clark Kent',
     * age: 60,
     * powers:['Super fuerza'],
     * getNombre(){
     *      return this.name;
     *  }
     * }
     */
})()